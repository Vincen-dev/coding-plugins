---
title: "<功能名称>技术方案"
status: draft
lifecycle_status: draft
feature: "<feature-name>"
doc_id: "<doc-id>"
created: YYYY-MM-DD
updated: YYYY-MM-DD
implemented_commits: []
validated_by: "<验证命令或人工验证记录>"
related_docs:
  - docs/coding-plugins/features/<feature-name>/requirements/<doc-id>-PRD.md
  - docs/coding-plugins/features/<feature-name>/test-cases/<doc-id>-TVD.md
  - docs/coding-plugins/features/<feature-name>/plans/<doc-id>-TED.md
  - docs/coding-plugins/features/<feature-name>/evidences/<doc-id>-VED.md
---

# <功能名称>技术方案

## 阅读摘要

- **本文结论:** <2 到 5 句话说明最终技术方案。>
- **当前状态:** 草稿，等待技术方案确认。
- **先读重点:** 先看方案摘要、规格缺口审查、规格到方案映射和关键决策。
- **下游同步:** TSD 更新后必须同步同一 `doc_id` 的 TVD、TED 和 VED，至少更新 `updated` 表示已评审。

> 写作门禁：真实文档不得保留任何 `<...>`、`YYYY-MM-DD` 或泛化占位；每个“不适用”都要说明原因。

## 文档信息

- 状态：草稿。
- 生命周期：draft。
- Feature：<feature-name>。
- Doc ID：<doc-id>。
- 文档类型：TSD / 技术方案文档。
- 已实现提交：[]。
- 验证方式：<验证命令或人工验证记录>。

关联关系只维护在 frontmatter `related_docs` 和 `docs/coding-plugins/INDEX.md`。正文不重复 PRD、TVD、TED、VED 的路径清单，只说明技术方案本身需要的决策、映射、实现边界和交接约束。

## 方案摘要

<2 到 5 句话说明整体技术方案。必须写出核心方案、边界、不做什么、主要风险或验证方式。>

## 规格缺口审查

- 未覆盖需求：<无；如存在，停止本技术方案并回到 spec 更新。>
- 验收标准不清：<无；如存在，停止本技术方案并回到 spec 更新。>
- 新增外部行为：<无；如存在，停止本技术方案并回到 spec 更新。>
- 错误边界 / 兼容要求：<无；如存在，停止本技术方案并回到 spec 更新。>
- 处理状态：<通过，未发现需要回写 spec 的缺口。>

## 规格到方案映射

| 规格 ID | 规格摘要 | 技术落点 | 关键决策 ID | 影响文件/符号 | 验证命令 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| REQ-001 | <规格摘要> | `<path>::<symbol>` 或 <具体模块落点；不得写“见下文”> | TD-001 | `<path>` | <测试命令或测试名称> | VED 证据，路径以 metadata/INDEX 为准 |

## 无需技术方案的规格

- 无：<如所有 MUST 规格都有技术落点，保留本项。>
- <规格 ID>：<仅当某个 MUST 规格确实不需要技术方案时填写；说明为什么不需要、如何验证、后续什么条件会触发技术方案。>

## 关键决策

| 决策 ID | 决策 | 原因 | 取舍 |
| --- | --- | --- | --- |
| TD-001 | <技术决策> | <为什么这么做> | <代价或风险> |

## 备选方案

- 方案 A：<被选择或被拒绝的方案；说明适用场景、成本和风险。>
- 方案 B：<如无有意义备选方案，说明为什么现有方案是唯一合理路径。>
- 取舍结论：<明确最终选择，并引用对应 TD ID。>

## 实现方案

本文直接承载实现层面的方案，避免为轻量变更生成第二份实现文档：

- 实现模式：<code / no_code；如 no_code，说明为什么只维护文档或配置边界。>
- 关联决策：<TD-001、TD-002 等。>
- 实现点：<模块、接口、数据结构、迁移或无代码边界。>
- 不写入本文的内容：<需求重定义、实际测试输出、TED 任务步骤等。>

## 影响组件

- `<path>::<symbol>`：<说明该组件为什么受影响、怎么变、覆盖哪些 Spec ID。>
- `<path>`：<没有稳定 symbol 时写到具体文件或配置项；说明影响范围和边界。>
- 不适用：<如果没有代码或配置组件受影响，说明原因。>

## 数据流 / 控制流

<核心流程，必要时使用 Mermaid。即使没有复杂流程，也要说明“不适用”的原因，例如纯文档链路、无运行时控制流。>

## 接口和契约

<内部接口、外部 API、schema、状态机如何落地。新增必须、禁止、MUST、SHOULD 类约束时，引用对应 Spec ID；如果只是实现内部限制，标注“设计约束”。>

## 非功能设计

- 性能：<性能影响、复杂度、缓存、批量处理或“不适用”的原因。>
- 安全 / 隐私：<权限、敏感数据、日志脱敏、访问边界或“不适用”的原因。>
- 可靠性 / 可观测性：<错误处理、重试、监控、日志、诊断或“不适用”的原因。>
- 可维护性：<模块边界、扩展点、删除旧逻辑、文档或“不适用”的原因。>

## 迁移 / 兼容性

<迁移、兼容、回滚、灰度；不适用时说明原因，以及什么条件下会变为适用。>

## 上线 / 回滚

- 上线方式：<发布、配置、迁移、灰度或 no-op；不适用时说明原因。>
- 回滚方式：<恢复步骤、回滚命令、数据修复或降级策略。>
- 回滚验证：<回滚后需要运行的命令、检查点或人工验收。>

## 测试策略

<按 Spec ID 说明测试层级、RED/GREEN 命令和 TDD 证据记录方式。不要记录实际测试输出；实际证据写入 VED。>

- REQ-001：<测试层级、关键断言、RED 命令、GREEN 命令、证据写入方式。>
- 不适用：<如无自动测试，说明 TDD 例外前置条件和替代验证。>

## 风险和缓解

- <风险>：<缓解方案；必须能落到设计、实现、测试或回滚动作。>
- <回滚 / 降级>：<说明触发条件、恢复方式和验证方式；不适用时说明原因。>
