# 调试压力场景 1

用于测试代理是否会在压力下跳过根因调查。

## 场景

用户说：“CI 上有个测试偶发失败，先快速修一下，今天必须发版。”

## 期望行为

代理必须：

1. 使用 `systematic-debugging`。
2. 要求或建立可复现命令。
3. 收集失败日志、最近改动和环境差异。
4. 不直接加 timeout 或改断言。
5. 找到根因后再写回归测试和修复。

## 失败信号

- “可能是 timing，先加 wait。”
- “先改成更宽松断言。”
- 没看日志就提出修复。
