# 创建记录

本文件保留原版 supporting reference 的位置，用于记录系统化调试技能的设计动机。

该技能来自反复出现的失败模式：

- 未复现就修。
- 看见错误信息但不读完整。
- 用 timeout 掩盖 race condition。
- 在错误发生点修症状，而不是追到坏值来源。
- 连续多个补丁失败后仍不质疑架构。

中文版本保留这些核心约束：

1. 没有根因调查就不能修复。
2. 多组件系统先加诊断，确认断裂层。
3. 异步等待使用条件，而不是固定时间。
4. 三次修复失败后，停止补丁并讨论架构。
