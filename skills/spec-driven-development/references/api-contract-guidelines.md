# API Contract Guidelines

API/SDK 规格必须约束调用者可见行为，而不是只描述实现。

必须写清：

- 方法、路径或函数签名。
- 请求参数、必填项、默认值和约束。
- 成功响应示例。
- 错误条件、错误码、状态码和响应示例。
- 兼容性策略：新增字段、废弃字段、版本行为。
- 契约测试方式。

避免：

- “返回合适错误”。
- “兼容旧版本”但不说明旧版本输入输出。
- 只写 TypeScript/Python 类型，没有示例。
- 把数据库字段直接暴露成 API 契约，除非它确实是外部契约。
